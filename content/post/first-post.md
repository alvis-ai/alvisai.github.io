+++
title = "用 Zola 10 分钟上线博客"
date = 2025-11-08
updated = 2025-11-08
[taxonomies]
tags = ["Rust", "Go"]
+++

这里是 Markdown 正文，支持代码高亮：

```rust
fn main() {
    println!("Hello, Zola!");
}
```

<!-- more -->

继续写下去就可以把整篇教程补充完整，可以放更多代码示例：

```rust
fn main() {
    println!("Hello, Zola!");
    println!("Welcome to the blog demo.");
}
```

也可以加入一些长段落来模拟真实文章。下面内容是为了展示较长的正文被折叠后仍能点击进入详情查看：

```rust
fn calculate() -> u32 {
    (0..10).map(|x| x * 2).sum()
}

fn main() {
    println!("sum: {}", calculate());
}
```
